// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// /**
//  * @file        schema.prisma
//  * @description Data model of Streamx24 project here you can easily understand
//  *              how streamx24.in application workflow works
//  * @author      Rohit Bhure
//  * @created     2026-05-26
//  * @updated     2026-05-26
//  * @version     1.0.0
//  */

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

generator erd {
  provider        = "prisma-erd-generator"
  output          = "./ERD.svg"
  puppeteerConfig = "./db/puppeteer-config.json"
}

// --------------------------------------

model User {
  id             Int           @id @default(autoincrement())
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt
  name           String?
  mobileNumber   String        @unique
  accountStatus  AccountStatus @default(ACTIVE)
  email          String        @unique
  hashedPassword String?
  role           Role          @default(USER)
  referCode      String        @unique

  banned     Boolean?   @default(false)
  banReason  BanReason?
  banExpires DateTime?

  // Geography — for analytics dashboards
  country String @default("INDIA")
  state   String
  city    String

  // Flags
  isProfileComplete Boolean @default(false)

  tokens   Token[]
  sessions Session[]
}

model Session {
  id                 Int       @id @default(autoincrement())
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  expiresAt          DateTime?
  handle             String    @unique
  hashedSessionToken String?
  antiCSRFToken      String?
  publicData         String?
  privateData        String?

  user   User? @relation(fields: [userId], references: [id])
  userId Int?
}

model Token {
  id          Int       @id @default(autoincrement())
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  hashedToken String
  type        TokenType
  expiresAt   DateTime
  sentTo      String

  user   User @relation(fields: [userId], references: [id])
  userId Int

  @@unique([hashedToken, type])
}

model Project {
  id          Int      @id @default(autoincrement())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  description String
}

// /* ─────────────────────────────────────────
//   Enum Section
// ───────────────────────────────────────── */

enum TokenType {
  RESET_PASSWORD
}

enum Role {
  USER
  PLAYER
  TEAM_OWNER
  TEAM_MANAGER
  COACH
  ANALYST
  TOURNAMENT_ORGANIZER
  MODERATOR
  SUPPORT
  STREAMER
  CASTER
  SPONSOR
  VERIFIED_USER
  CONTENT_CREATOR
  ADMIN
  SUPER_ADMIN
}

enum AccountStatus {
  ACTIVE
  SUSPENDED
  BANNED
  PENDING_VERIFICATION
}

enum GameType {
  PUBG
  BGMI
  FREE_FIRE
  COD
  VALORANT
  FORTNITE
  DOTA_2
  CS2
}

enum MatchMode {
  SOLO
  DUO
  SQUAD
}

enum TournamentStatus {
  UPCOMING
  REGISTRATION_OPEN
  REGISTRATION_CLOSED
  ROOM_CREATED
  LIVE
  RESULT_PENDING
  COMPLETED
  CANCELLED
}

enum ParticipantStatus {
  JOINED
  LEFT
  DISQUALIFIED
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
  REFUNDED
}

enum BalanceBucket {
  MAIN
  WINNING
  BONUS
}

enum WalletLedgerType {
  DEPOSIT
  TOURNAMENT_JOIN
  WINNING
  BONUS
  WITHDRAWAL
  REFUND
  PENALTY
  REFERRAL_BONUS
}

enum WithdrawalStatus {
  PENDING
  APPROVED
  REJECTED
  PAID
}

enum KYCStatus {
  PENDING
  VERIFIED
  REJECTED
}

enum BanReason {
  CHEATING
  FRAUD
  MULTI_ACCOUNT
  ABUSE
  SUSPICIOUS_ACTIVITY
  OTHER
}

enum BanType {
  TEMPORARY
  PERMANENT
}

enum NotificationType {
  TOURNAMENT_START
  TOURNAMENT_RESULT
  WALLET_CREDIT
  WALLET_DEBIT
  KYC_UPDATE
  WITHDRAWAL_UPDATE
  REFERRAL_REWARD
  SYSTEM
  BAN
  TICKET_UPDATE
  DISPUTE_UPDATE
  PROMO_APPLIED
}

enum TeamRole {
  CAPTAIN
  MEMBER
}

enum ReferralRewardStatus {
  PENDING
  REWARDED
  FAILED
}

enum GameProfileStatus {
  PENDING
  VERIFIED
  REJECTED
}

enum DevicePlatform {
  ANDROID
  IOS
  WEB
  WINDOWS
  MACOS
  LINUX
  TABLET
  SMART_TV
  DESKTOP
  UNKNOWN
}

enum TicketStatus {
  OPEN
  IN_PROGRESS
  WAITING_FOR_USER
  RESOLVED
  CLOSED
}

enum TicketPriority {
  LOW
  MEDIUM
  HIGH
  URGENT
}

enum TicketCategory {
  PAYMENT_ISSUE
  WITHDRAWAL_ISSUE
  TOURNAMENT_DISPUTE
  ACCOUNT_ISSUE
  KYC_ISSUE
  TECHNICAL_BUG
  CHEATING_REPORT
  REWARD_NOT_RECEIVED
  GENERAL_INQUIRY
  OTHER
}

enum TicketMessageSender {
  USER
  SUPPORT
  SYSTEM
}

enum RoundStatus {
  UPCOMING
  LIVE
  COMPLETED
  CANCELLED
}

enum DisputeStatus {
  PENDING
  UNDER_REVIEW
  ACCEPTED
  REJECTED
}

enum DiscountType {
  FLAT
  PERCENT
}

enum BatchStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
}

enum ReportReason {
  CHEATING
  HACKING
  ABUSIVE_LANGUAGE
  INAPPROPRIATE_NAME
  MULTI_ACCOUNT
  MATCH_FIXING
  OTHER
}

enum ReportStatus {
  PENDING
  REVIEWED
  ACTIONED
  DISMISSED
}

enum ConfigDataType {
  STRING
  NUMBER
  BOOLEAN
  JSON
}
